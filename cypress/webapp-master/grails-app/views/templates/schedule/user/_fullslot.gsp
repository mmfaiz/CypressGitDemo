<td class="slot ${slotpast ? 'not-available':''} ${color}"
    rel="tooltip"
    title="${slotpast ? message(code: 'schedule.slot.passed') : message(code: 'schedule.slot.not.available')}"
    data-delay="350"
    data-html="true"
    data-container="body"
    style="position:inherit;">
</td>
